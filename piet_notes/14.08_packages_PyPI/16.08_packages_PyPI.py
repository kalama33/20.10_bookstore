import datetime # built in package

print(datetime)


import numpy # package installed via pip

print(numpy)


import ansible # install via sudo apt-add-repository ppa:ansible/ansible

print(ansible)
